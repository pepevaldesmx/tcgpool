/**
 * El nombre del servicio, en UN solo lugar.
 *
 * Está sin decidir —y el dominio también— así que vive aquí y no repartido por
 * ocho pantallas: cambiarlo tiene que costar una línea, no una cacería. Por eso
 * tampoco se escribe el nombre en textos corridos como si fuera prosa; se
 * interpola.
 */
export const MARCA = "Tecegetitlán";

/** Para el `<title>`: el nombre más lo que somos, que es lo que busca la gente. */
export const MARCA_TITULO = `${MARCA} — busca cartas en tiendas mexicanas`;
